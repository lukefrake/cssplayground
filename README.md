# CSS Playground

A work in progress for people to come and play with the most up to date CSS.

# Developing

` $ bundle install `

Compile SASS

`gulp sass`

Run the server:

` $ jekyll serve `

And visit...

`http://localhost:1234/`
